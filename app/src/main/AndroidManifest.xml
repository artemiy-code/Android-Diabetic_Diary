<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_heartbeat"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_heartbeat_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiabetesDiary">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.measurement.MeasurementsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.statistics.StatisticsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.products.ProductsActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.foodlog.FoodLogActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.reminders.RemindersActivity"
            android:screenOrientation="portrait" />

        <receiver
            android:name=".notifications.ReminderReceiver"
            android:exported="false" />
    </application>

</manifest>